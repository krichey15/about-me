'use strict';

var name;
var origin;
var age;
var bae;
var home;
var correct = 0;

while (name.toUpperCase() !== 'YES' || 'Y') {
  name = prompt('This is the first of five YES or NO questions to unlock my life. \nHere is is a gimme\'. \n\nIs my name Kyle?\n\n\(Hint:YES\)');
  if (name.toUpperCase() === 'YES' || 'Y') {
    break;
  }
  alert('Hint: See Hint!');
  console.log('More than one try.')
}
correct++;
console.log('Understood my hint');
console.log(correct);

while (origin.toUpperCase() !== 'YES' || 'Y') {
  origin = prompt('')
  if (orign.toUpperCase() === 'YES' || 'Y') {
    break;
  }
  alert();
  console.log();
}
correct++
console.log();
console.log();

while (origin.toUpperCase() !== 'YES' || 'Y') {
  origin = prompt('')
  if (orign.toUpperCase() === 'YES' || 'Y') {
    break;
  }
  alert();
  console.log();
}
correct++
console.log();
console.log();

while (origin.toUpperCase() !== 'YES' || 'Y') {
  origin = prompt('')
  if (orign.toUpperCase() === 'YES' || 'Y') {
    break;
  }
  alert();
  console.log();
}
correct++
console.log();
console.log();

while (origin.toUpperCase() !== 'YES' || 'Y') {
  origin = prompt('')
  if (orign.toUpperCase() === 'YES' || 'Y') {
    break;
  }
  alert();
  console.log();
}
correct++
console.log();
console.log();
